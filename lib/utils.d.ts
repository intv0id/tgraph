import * as THREE from 'three';
export declare function makeMaterial(color: string, shaderName?: string): THREE.ShaderMaterial;
export declare function extend<First, Second>(first: First, second: Second): First & Second;
