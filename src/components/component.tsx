/*import * as React from "react";
export { Graph, GraphEdge, GraphNode } from "../graphTypes";

export interface GraphProps { nodes:  }

export class GraphCanvas extends React.Component<GraphProps, {}> {
    render() {
        return <h1>Hello from {this.props.compiler} and {this.props.framework}!</h1>;
    }
}*/


/**
 * IDEAS
 * PROPS: GraphData
 * STATE: Selected Node (use the details display component in the reducer, this component should probably be included in the state as well)
 * Eliminate completely the use of JQuery 
 */